section()
  header.header
    span.header-logo
    span.hamburger(ng-click="showSidebar = !showSidebar")
      span
  nav.sidebar(ng-class="{'is-visible': showSidebar}")
    li.sidebar-label Menu
    menu(content='userMenu')

  .main-content
    .content-wrapper
      div(ui-view="")

